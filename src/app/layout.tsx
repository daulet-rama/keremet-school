/**
 * Корневой макет намеренно пустой: <html> и <body> объявляются
 * в src/app/[locale]/layout.tsx, потому что атрибут lang зависит
 * от языка маршрута.
 *
 * Это не мелочь. В исследовании почти каждый двуязычный школьный сайт
 * ошибался именно здесь: казахская страница объявлена lang="ru-RU",
 * английская — lang="zh-CN". Ломается озвучка в скринридере, предложение
 * перевода в браузере и языковой таргетинг в поиске.
 */
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
